import { Layout } from "@components/layout";
import { Form } from "../../components/form";

export const Login = () => {
  return (
    <Layout>
            <Form type="login" />
    </Layout>
  );
};
