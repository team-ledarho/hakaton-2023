import { Layout } from '@components/layout';
import { Form } from '../../components/form';

export const Register = () => {
  return (
    <Layout>
      <Form type="register" />
    </Layout>
  );
};
