{
  "name": "hakaton-2023",
  "author": "ledarho",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "develop:back": "yarn --cwd back develop",
    "develop:front": "yarn --cwd front start",
    "develop": "cross-env FORCE_COLOR=1 npm-run-all -l -p develop:*",
    "inst": "yarn install && cd front && yarn install && cd .. && cd back && yarn install"
  },
  "devDependencies": {
    "cross-env": "7.0.3",
    "npm-run-all": "4.1.5",
    "wait-on": "7.0.1"
  }
}
